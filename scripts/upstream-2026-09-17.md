# Upstream maintenance - 2026-09-17

Merged upstream through ccba72caac46e807225008a9a3232eed72737bb4 (39 commits): fish data and future fishing windows, spearfishing, AutoHook presets, gearsets, quick synthesis, vendors and Raphael validation.

Enhanced build-time patches remain in place for material acquisition, supply preferences, retainer recovery, manual travel and sanctuary return. Inventory isolation now wraps GetTotalCount(bool), preserving upstream per-list policy while keeping Vulcan targets bag-only through both overloads. Upstream dismisses retainer greetings itself; validation accepts it without injecting a duplicate handler.

Full patched Release build: zero errors, existing warnings remain. Both workflow verification blocks and ten inventory regression checks passed locally. The tests compile the patched production GatherableExtensions.cs against detached inventory/IPC doubles. They cover ordinary counts, explicit bag-only counts, both overloads with Vulcan active, unrelated items, disabled lists, user preferences, unavailable IPC and collectables.

Apply patches in the order in .github/workflows/fork-build-publish.yml, then run `dotnet run --project tests/ForkInventory.Tests -c Release`. CI also runs these tests and validates the packaged identity/version. Distribution metadata now comes from the built manifest.

In-game fishing, retainer dialogs, navigation and crafting remain untested in a running client.

## Upstream overlap review

- Retainer greeting: upstream now owns the Talk handler. Removed the former conditional injection entirely.
- CraftingGameInterop unsafe declarations: both helpers are already fixed upstream. Removed the two compatibility rewrites.
- CI checks the patched output for exactly one greeting lookup/click and one declaration per helper; it rejects reintroduction of the retired injection.
- Inventory isolation is only partially covered upstream: ActiveItemList and primary node selection pass UsesRetainerInventory, but CollectableRotation.GetNextAction and both completed-list cleanup paths still call the legacy overload. The Enhanced guard remains necessary for those paths; it returns the bag count before the upstream retainer path, so the two implementations do not perform duplicate inventory operations.
- Bell reacquisition/live inventory scanning, queue-stage ownership, bounded material recovery, grouped vendors/preferences, manual travel, sanctuary return and passive status are not supplied by the merged upstream. Their existing Enhanced patches remain.
